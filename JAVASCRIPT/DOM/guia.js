/**
 * - Dada una secuencia de numeros cuantos numeros son multiplos de 15
 * - Crear la simulacion de una calculadora, el usuario debe ingresar 2 numeros y hacer el tipo de operacion que corresponda
 */

[1,2,3,4,5,10]

if(numero % 15 === 0){
    //es multiple de 15
}

const paragraph = 'The quick brown fox jumps over the lazy dog. It barked.';
const regex = /[0-9]/g;
const found = paragraph.match(regex);